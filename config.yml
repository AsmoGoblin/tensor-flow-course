title: Tensor Flow
description: Image processing using tensor flow
template:
  repo: tensor-flow-template
  name: tensor-flow
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
